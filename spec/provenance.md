# AgentEnvelope Provenance Record

This document is informative. It records public provenance for the AgentEnvelope category framing
and early invention declaration. It is not a substitute for the normative protocol specification in
[agent-envelope-v1.md](./agent-envelope-v1.md), and it is not legal advice.

## On-Chain Declaration

An AgentEnvelope declaration was published on Ethereum Mainnet with the title:

```text
AgentEnvelope - First Declaration of Invention and Category Claim
```

Recorded timestamp:

```text
2026-07-24 15:21:54
```

Known public metadata from the declaration:

| Field | Value |
| --- | --- |
| Network | Ethereum Mainnet |
| Contract | `0x939...fDdE5` |
| Deployer | `0x616...f434a` |
| Signer | `0x616EeA...41Bf434a` |
| Author | Matthew McPhillips |
| Domain | `agentenvelope.io` |
| Category phrase | IAM for Autonomous Systems |
| Status phrase | First Public Declaration |

Before external publication, replace abbreviated addresses with full contract, deployer, signer,
transaction, and block details.

## Public Purpose

The declaration is a public timestamp and authorship marker for the AgentEnvelope category framing:

```text
Authority is not issued. Authority is derived.
```

It records an early public claim around deterministic, domain-first authority for autonomous and
action-performing systems.

## Boundary

The on-chain declaration should be cited as provenance only.

The protocol itself is defined by:

- the normative v1 specification;
- the deterministic test vectors;
- the SDK and compatible implementations;
- public records and verification behavior.

The on-chain record should not be described as proving legal ownership, novelty, patentability, or
exclusive rights over a broad category without advice from qualified counsel.
